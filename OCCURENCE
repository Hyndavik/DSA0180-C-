#include <iostream>
using namespace std;
int main() {
    int n, value, count = 0;
    cout << "Enter the number of elements in the array: ";
    cin >> n;
    int arr[n];
    cout << "Enter " << n << " elements: ";
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }
    cout << "Enter the value to search for: ";
    cin >> value;
    for (int i = 0; i < n; i++) {
        if (arr[i] == value) {
            count++;  
}
    }
    cout << "The value " << value << " appears " << count << " time(s) in the array." << endl;

    return 0;
}

OUTPUT
Enter the number of elements in the array: 6
Enter 6 elements: 10 20 20 30 20 40
Enter the value to search for: 20
The value 20 appears 3 time(s) in the array.
